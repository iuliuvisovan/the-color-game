import { StyleSheet, Dimensions } from 'react-native';

export default StyleSheet.create({
  blinker: {
    height: 18,
    marginTop: 4,
    borderRightWidth: 1,
    width: 1,
    marginLeft: 2
  }
});
